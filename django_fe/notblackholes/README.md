# Type to run the Settings for the Dev Server:
$ python manage.py runserver --settings=notblackhole.settings --configurations=Dev

# For the Prod Server:
$ python manage.py runserver --settings=notblackhole.settings --configurations=Prod
